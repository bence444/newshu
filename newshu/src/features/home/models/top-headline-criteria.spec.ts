import { TopHeadlineCriteria } from './top-headline-criteria';

describe('TopHeadlineCriteria', () => {
  it('should create an instance', () => {
    expect(new TopHeadlineCriteria()).toBeTruthy();
  });
});
